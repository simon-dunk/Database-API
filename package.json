{
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.556.0",
    "@aws-sdk/lib-dynamodb": "^3.556.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fs": "^0.0.1-security",
    "node": "^18.20.2",
    "nodemon": "^3.1.0",
    "uuid": "^9.0.1"
  },
  "scripts": {
    "install-packages": "npm run --silent logNode && npm install node && npm run --silent logNodemon && npm install nodemon && npm run --silent logExpress && npm install express && npm run --silent logAwsClientDB && npm install @aws-sdk/client-dynamodb && npm run --silent logAwsLibDB && npm install @aws-sdk/lib-dynamodb && npm run --silent logDotenv && npm install dotenv && npm run --silent logUuid && npm install uuid && npm run --silent logFs && npm install fs && npm run --silent logPy3Dotenv && pip3 install python-dotenv && npm run --silent logPyDotenv && pip install python-dotenv",
    "logNode": "echo [TERMINAL] Installing node",
    "logNodemon": "echo [TERMINAL] Installing nodemon",
    "logExpress": "echo [TERMINAL] Installing express",
    "logAwsClientDB": "echo [TERMINAL] Installing @aws-sdk/client-dynamodb",
    "logAwsLibDB": "echo [TERMINAL] Installing @aws-sdk/lib-dynamodb",
    "logDotenv": "echo [TERMINAL] Installing dotenv",
    "logUuid": "echo [TERMINAL] Installing uuid",
    "logFs": "echo [TERMINAL] Installing fs",
    "logPyDotenv": "echo [TERMINAL] Installing python-dotenv with pip",
    "logPy3Dotenv": "echo [TERMINAL] Installing python-dotenv with pip3"
  }
}
